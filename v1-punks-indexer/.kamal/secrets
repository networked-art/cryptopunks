KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# RPC endpoints
PONDER_RPC_URLS_1=$PONDER_RPC_URLS_1
PONDER_RPC_FALLBACK_URLS_1=$PONDER_RPC_FALLBACK_URLS_1
PONDER_WS_URL_1=$PONDER_WS_URL_1

# PostgreSQL password (used by both accessory and app)
POSTGRES_PASSWORD=$DB_PASSWORD

# Database connection string (points to the kamal db accessory)
DATABASE_URL=$DATABASE_URL

# Database schema (unique per deployment for zero-downtime deploys)
DATABASE_SCHEMA=$(git rev-parse HEAD)

# Stable views schema name
PONDER_VIEWS_SCHEMA=v1_punks

# PunksMarket deployment (read by utils/contracts.ts)
PUNKS_MARKET_ADDRESS=$PUNKS_MARKET_ADDRESS
PUNKS_MARKET_START_BLOCK=$PUNKS_MARKET_START_BLOCK
