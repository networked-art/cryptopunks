{
  "name": "@networked-art/punks.auction",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --port 3000",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxt typecheck",
    "kamal:setup": "set -a && . ./.env.production && set +a && kamal setup",
    "kamal:deploy": "set -a && . ./.env.production && set +a && kamal deploy",
    "kamal:sh": "set -a && . ./.env.production && set +a && kamal app exec -i --reuse sh"
  },
  "dependencies": {
    "@1001-digital/layers.evm": "^2.7.25",
    "@networked-art/punks-sdk": "workspace:*",
    "@resvg/resvg-js": "^2.6.2",
    "@takumi-rs/core": "^1.6.0",
    "@tanstack/vue-query": "5.95.2",
    "@vueuse/core": "^14.2.1",
    "@wagmi/core": "3.4.6",
    "@wagmi/vue": "0.5.6",
    "avvatars": "^0.1.0",
    "satori": "^0.26.0",
    "viem": "2.48.4"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "nuxt": "^4.4.2",
    "nuxt-og-image": "^6.5.1",
    "vue-tsc": "^3.2.6"
  }
}
